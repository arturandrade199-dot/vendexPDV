<ui:FluentWindow x:Class="Vendex.App.NovaContaPagarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Nova conta a pagar" Height="480" Width="420"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid Margin="24,0,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Nova conta a pagar" />

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <TextBlock Text="Descrição" Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding Descricao, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="Categoria" Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding Categoria, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Fornecedor, Aluguel, Energia..." Margin="0,0,0,12" />

            <TextBlock Text="Fornecedor (opcional)" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding Fornecedores}" DisplayMemberPath="Nome"
                      SelectedItem="{Binding FornecedorSelecionado}" Margin="0,0,0,12" />

            <TextBlock Text="Valor total (R$)" Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding ValorTotalTexto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="Vencimento" Margin="0,0,0,4" />
            <DatePicker SelectedDate="{Binding DataVencimento}" Margin="0,0,0,12" />

            <TextBlock Text="{Binding MensagemErro}" Foreground="#C4291E" Margin="0,0,0,12" TextWrapping="Wrap" />

            <ui:Button Content="Salvar" Appearance="Primary" Command="{Binding SalvarCommand}" HorizontalAlignment="Right" Padding="24,8" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
