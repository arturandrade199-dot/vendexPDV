<UserControl x:Class="Vendex.App.Views.ProdutosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Cartões de resumo -->
        <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,20">
            <Border Style="{StaticResource CartaoResumo}" BorderBrush="#3B82F6">
                <StackPanel>
                    <TextBlock Text="Total de produtos" Style="{StaticResource RotuloResumo}" />
                    <TextBlock Text="{Binding TotalProdutos}" Style="{StaticResource ValorResumo}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CartaoResumo}" BorderBrush="#2FA35C">
                <StackPanel>
                    <TextBlock Text="Ativos" Style="{StaticResource RotuloResumo}" />
                    <TextBlock Text="{Binding Ativos}" Style="{StaticResource ValorResumo}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CartaoResumo}" BorderBrush="#F0413A">
                <StackPanel>
                    <TextBlock Text="Estoque baixo" Style="{StaticResource RotuloResumo}" />
                    <TextBlock Text="{Binding EstoqueBaixo}" Style="{StaticResource ValorResumo}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CartaoResumo}" BorderBrush="#F2A93B" Margin="0">
                <StackPanel>
                    <TextBlock Text="Valor em estoque (custo)" Style="{StaticResource RotuloResumo}" />
                    <TextBlock Text="{Binding ValorEmEstoqueFormatado}" Style="{StaticResource ValorResumo}" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Barra de ações -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <ui:Button Content="+ Adicionar" Appearance="Primary"
                           Command="{Binding AdicionarCommand}" Padding="18,8"
                           HorizontalAlignment="Left" />
            </Grid>

            <!-- Tabela -->
            <Border Grid.Row="1" Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1">
                <ListView ItemsSource="{Binding Produtos}" BorderThickness="0" Background="Transparent">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Nome" Width="220">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Nome}" Foreground="#1F2430" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Código de barras" Width="140">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding CodigoBarras}" Foreground="#1F2430" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Custo" Width="110">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding PrecoCustoFormatado}" Foreground="#1F2430" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Venda" Width="110">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding PrecoVendaFormatado}" Foreground="#1F2430" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Estoque" Width="90">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Estoque}">
                                            <TextBlock.Foreground>
                                                <SolidColorBrush Color="{Binding EstoqueCor}" />
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Situação" Width="110">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="10" Padding="10,3" HorizontalAlignment="Left">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding SituacaoFundo}" />
                                            </Border.Background>
                                            <TextBlock Text="{Binding SituacaoTexto}" FontWeight="Medium">
                                                <TextBlock.Foreground>
                                                    <SolidColorBrush Color="{Binding SituacaoCor}" />
                                                </TextBlock.Foreground>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Ações" Width="220">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <ui:Button Content="Editar" Margin="0,0,8,0"
                                                       Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                       CommandParameter="{Binding}" />
                                            <ui:Button Content="{Binding RotuloAlternarAtivo}"
                                                       Command="{Binding DataContext.AlternarAtivoCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                       CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </Grid>
    </Grid>
</UserControl>
