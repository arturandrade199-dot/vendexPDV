using System.Windows.Controls;

namespace Vendex.App.Views;

/// <summary>Construída em código (nunca via navegação/XAML host) só na hora de imprimir —
/// ver ReciboView.CriarVisualImpressao().</summary>
public partial class ReciboTermicoView : UserControl
{
    public ReciboTermicoView()
    {
        InitializeComponent();
    }
}
