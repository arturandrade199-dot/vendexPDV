<UserControl x:Class="Vendex.App.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:Vendex.App.ViewModels"
             mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="Selecione um módulo" FontSize="14" Foreground="#6B7280" Margin="0,0,0,16" />

        <ItemsControl ItemsSource="{Binding Modulos}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ModuloTile}">
                    <Border Style="{StaticResource CartaoModulo}">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding Comando}" />
                        </Border.InputBindings>
                        <StackPanel>
                            <Border Width="44" Height="44" CornerRadius="10" Background="#EDEBFF" Margin="0,0,0,14">
                                <ui:SymbolIcon Symbol="{Binding Icone}" FontSize="20" Foreground="#5B4FE9"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="{Binding Nome}" FontWeight="SemiBold" FontSize="15"
                                       Foreground="#1F2430" Margin="0,0,0,4" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding Descricao}" Foreground="#6B7280" FontSize="12"
                                       TextWrapping="Wrap" />
                            <TextBlock Text="{Binding RotuloDisponibilidade}" Foreground="#B8860A" FontSize="11"
                                       FontWeight="Medium" Margin="0,10,0,0" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
