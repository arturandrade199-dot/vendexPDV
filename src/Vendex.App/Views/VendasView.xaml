<UserControl x:Class="Vendex.App.Views.VendasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Cartões de resumo -->
        <UniformGrid Grid.Row="0" Columns="2" Margin="0,0,0,20">
            <Border Style="{StaticResource CartaoResumo}" BorderBrush="#5B4FE9">
                <StackPanel>
                    <TextBlock Text="Vendas no período" Style="{StaticResource RotuloResumo}" />
                    <TextBlock Text="{Binding TotalVendas}" Style="{StaticResource ValorResumo}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CartaoResumo}" BorderBrush="#1B8A4B">
                <StackPanel>
                    <TextBlock Text="Faturado no período" Style="{StaticResource RotuloResumo}" />
                    <TextBlock Text="{Binding TotalFaturadoFormatado}" Style="{StaticResource ValorResumo}" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Filtro de período -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="De" Margin="0,0,0,4" Foreground="#6B7280" FontSize="12" />
                <DatePicker SelectedDate="{Binding DataInicio}" Width="140" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Até" Margin="0,0,0,4" Foreground="#6B7280" FontSize="12" />
                <DatePicker SelectedDate="{Binding DataFim}" Width="140" />
            </StackPanel>

            <ui:Button Grid.Column="4" Content="Buscar" Appearance="Primary" Padding="18,8"
                       VerticalAlignment="Bottom" Command="{Binding BuscarCommand}" IsEnabled="{Binding PodeBuscar}" />
        </Grid>

        <!-- Tabela -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1">
            <ListView ItemsSource="{Binding Vendas}" BorderThickness="0" Background="Transparent">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Venda" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NumeroFormatado}" Foreground="#1F2430" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Data/Hora" Width="140">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DataHoraFormatada}" Foreground="#1F2430" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Cliente" Width="160">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ClienteTexto}" Foreground="#1F2430" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Forma de pagamento" Width="180">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FormaPagamentoResumo}" Foreground="#1F2430" TextWrapping="Wrap" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Operador" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding UsuarioNome}" Foreground="#1F2430" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Total" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TotalFormatado}" Foreground="#1F2430" FontWeight="SemiBold" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Ações" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <ui:Button Content="Ver itens / Reimprimir"
                                               Command="{Binding DataContext.VerDetalhesCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                               CommandParameter="{Binding}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
