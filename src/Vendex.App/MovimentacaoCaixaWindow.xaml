<ui:FluentWindow x:Class="Vendex.App.MovimentacaoCaixaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="{Binding Titulo}" Height="380" Width="400"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid Margin="24,0,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{Binding Titulo}" />

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <TextBlock Text="Valor (R$)" Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding ValorTexto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="Motivo" Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding Motivo, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"
                        AcceptsReturn="True" TextWrapping="Wrap" Height="70" />

            <TextBlock Text="{Binding MensagemErro}" Foreground="#C4291E" Margin="0,0,0,12" TextWrapping="Wrap" />

            <ui:Button Content="Confirmar" Appearance="Primary" Command="{Binding ConfirmarCommand}"
                       HorizontalAlignment="Right" Padding="24,8" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
