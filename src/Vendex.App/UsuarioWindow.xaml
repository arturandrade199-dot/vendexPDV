<ui:FluentWindow x:Class="Vendex.App.UsuarioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="{Binding Titulo}" Height="640" Width="420"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid Margin="24,0,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{Binding Titulo}" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,16,0,0">
                <TextBlock Text="Nome" Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                <TextBlock Text="Login" Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                <TextBlock Text="{Binding RotuloSenha}" Margin="0,0,0,4" />
                <PasswordBox x:Name="PasswordBoxSenha" Margin="0,0,0,12" PasswordChanged="PasswordBoxSenha_PasswordChanged" />

                <CheckBox Content="Administrador (acesso irrestrito)" IsChecked="{Binding EhAdministrador}" Margin="0,0,0,12" />

                <StackPanel Visibility="{Binding EhAdministrador, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <TextBlock Text="Módulos permitidos" FontWeight="SemiBold" Margin="0,4,0,10" Foreground="#1F2430" />
                    <ItemsControl ItemsSource="{Binding ModulosDisponiveis}" Margin="0,0,0,12">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding NomeModulo}" IsChecked="{Binding Permitido}" Margin="0,0,0,8" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <TextBlock Text="{Binding MensagemErro}" Foreground="#C4291E" Margin="0,0,0,12" TextWrapping="Wrap" />

                <ui:Button Content="Salvar" Appearance="Primary" Command="{Binding SalvarCommand}"
                           HorizontalAlignment="Right" Padding="24,8" Margin="0,0,0,16" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
