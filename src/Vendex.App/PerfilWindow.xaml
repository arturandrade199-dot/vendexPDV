<ui:FluentWindow x:Class="Vendex.App.PerfilWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Meu perfil" Height="420" Width="380"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid Margin="24,0,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Meu perfil" />

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <Grid Width="96" Height="96" HorizontalAlignment="Center" Margin="0,0,0,16">
                <Border CornerRadius="48" Background="#5B4FE9">
                    <TextBlock Text="{Binding IniciaisNome}" Foreground="White" FontSize="32" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <Ellipse Visibility="{Binding CaminhoFoto, Converter={StaticResource NullToVisibilityConverter}}">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding CaminhoFoto, Converter={StaticResource PathToImageSourceConverter}}" Stretch="UniformToFill" />
                    </Ellipse.Fill>
                </Ellipse>
            </Grid>

            <ui:Button Content="Trocar foto..." Command="{Binding EscolherFotoCommand}"
                       HorizontalAlignment="Center" Margin="0,0,0,20" />

            <TextBlock Text="Nome" Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="{Binding MensagemErro}" Foreground="#C4291E" Margin="0,0,0,12" TextWrapping="Wrap" />

            <ui:Button Content="Salvar" Appearance="Primary" Command="{Binding SalvarCommand}"
                       HorizontalAlignment="Right" Padding="24,8" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
